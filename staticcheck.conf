# staticcheck config — what we suppress and why.
#
# ST1003 (underscores in package names):
#   examples/* packages match directory names (saas_tiers /
#   credit_topup / cohort_course / included_quota / per_seat_metered)
#   for "directory == example name" readability. No non-example
#   package in the lib uses underscores; reintroduce ST1003 if that
#   convention ever changes.
checks = ["all", "-ST1003"]
